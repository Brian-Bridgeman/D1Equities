<UserControl x:Class="D1Equities.GUI.View.MarketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:D1Equities.GUI.View"
             xmlns:viewModel="clr-namespace:D1Equities.GUI.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="1200">

    <UserControl.DataContext>
        <viewModel:MarketViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="540" />
        </Grid.ColumnDefinitions>


        <Border Grid.Column="0"
                CornerRadius="10"
                Padding="20"
                BorderBrush="#333"
                BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#191919" Offset="0"/>
                    <GradientStop Color="#121212" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBox Width="250"
                             Height="32"
                             VerticalAlignment="Center"
                             Padding="10,5"
                             FontSize="16"
                             Background="#2A2A2A"
                             Foreground="White"
                             BorderBrush="#444"
                             BorderThickness="1"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                </DockPanel>

                <ListBox Grid.Row="1"
                     ItemsSource="{Binding Stocks}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling"
                     ScrollViewer.IsDeferredScrollingEnabled="True"
                     HorizontalContentAlignment="Stretch">

                    <ListBox.Resources>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.Resources>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <RadioButton Style="{StaticResource stockButton}"
                         Command="{Binding DataContext.ShowStockViewCommand,
                                   RelativeSource={RelativeSource AncestorType=Window}}"
                         CommandParameter="{Binding Ticker}">
                                <Grid Margin="10,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                               Text="{Binding Symbol}"
                               Foreground="White"
                               FontSize="20"
                               FontWeight="Medium"/>

                                    <TextBlock Grid.Column="1"
                               Text="{Binding Name}"
                               Foreground="Gray"
                               FontSize="16"
                               FontWeight="Normal"
                               Margin="12,0,0,0"/>
                                </Grid>
                            </RadioButton>
                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>

            </Grid>
        </Border>



        <Grid Grid.Column="1" Margin="10,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>


            <Border Grid.Column="0"
                    CornerRadius="10"
                    Padding="15"
                    BorderBrush="#333"
                    BorderThickness="1"
                    Margin="0,0,10,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#191919" Offset="0"/>
                        <GradientStop Color="#121212" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <StackPanel>
                    <TextBlock Text="Market Movers"
                               Foreground="White"
                               FontSize="16"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding MarketMovers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,12">

                                        <TextBlock Text="{Binding Symbol}"
                                           Foreground="White"
                                           FontWeight="Bold"
                                           FontSize="14"
                                           Margin="0,0,0,4"/>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="70" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="80" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="0,0,20,0">
                                                <TextBlock Text="$" Foreground="White"/>
                                                <TextBlock Text="{Binding Price}" Foreground="White"/>
                                            </StackPanel>

                                            <StackPanel Orientation="Horizontal" Grid.Column="1" Margin="0,0,20,0">
                                                <TextBlock Text="+$" Foreground="{StaticResource D1Green}"/>
                                                <TextBlock Text="{Binding Change}" Foreground="{StaticResource D1Green}"/>
                                            </StackPanel>

                                            <StackPanel Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Left">
                                                <TextBlock Text="+" Foreground="{StaticResource D1Green}"/>
                                                <TextBlock Text="{Binding PercentChange}" Foreground="{StaticResource D1Green}"/>
                                                <TextBlock Text="%" Foreground="{StaticResource D1Green}"/>
                                            </StackPanel>

                                        </Grid>

                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>



            <Border Grid.Column="1"
                    CornerRadius="10"
                    Padding="15"
                    BorderBrush="#333"
                    BorderThickness="1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#191919" Offset="0"/>
                        <GradientStop Color="#121212" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <StackPanel>
                    <TextBlock Text="Most Active"
                               Foreground="White"
                               FontSize="16"
                               FontWeight="Bold"
                               Margin="0,0,0,10"/>

                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding MostActive}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,10">
                                        <TextBlock Text="{Binding Symbol}"
                                                   Foreground="White"
                                                   FontWeight="Bold"/>

                                        <StackPanel Orientation="Horizontal" Margin="0,9,0,0">
                                            <TextBlock Text="Vol:" Foreground="Gray"/>
                                            <TextBlock Text="{Binding Volume}"
                                                       Foreground="White" Margin="5,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

        </Grid>
    </Grid>

</UserControl>
